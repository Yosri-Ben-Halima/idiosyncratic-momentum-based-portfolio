version: "3"

tasks:

  install:
    desc: "Automatically detect and install Python dependencies"
    cmds:
      # generate requirements.txt using pipreqs (overwrite if exists)
      - pipreqs . --force
      # install all dependencies
      - pip install -r requirements.txt